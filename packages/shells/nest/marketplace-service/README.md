# shells-nest-marketplace-service

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build shells-nest-marketplace-service` to build the library.

## Running unit tests

Run `nx test shells-nest-marketplace-service` to execute the unit tests via [Jest](https://jestjs.io).
