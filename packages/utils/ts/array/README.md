# utils-ts-array

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build utils-ts-array` to build the library.

## Running unit tests

Run `nx test utils-ts-array` to execute the unit tests via [Jest](https://jestjs.io).
