# utils-ts-type

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build utils-ts-type` to build the library.

## Running unit tests

Run `nx test utils-ts-type` to execute the unit tests via [Jest](https://jestjs.io).
