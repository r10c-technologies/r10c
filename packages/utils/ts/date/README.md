# utils-ts-date

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build utils-ts-date` to build the library.

## Running unit tests

Run `nx test utils-ts-date` to execute the unit tests via [Jest](https://jestjs.io).
