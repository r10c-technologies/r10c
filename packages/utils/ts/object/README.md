# utils-ts-object

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build utils-ts-object` to build the library.

## Running unit tests

Run `nx test utils-ts-object` to execute the unit tests via [Jest](https://jestjs.io).
