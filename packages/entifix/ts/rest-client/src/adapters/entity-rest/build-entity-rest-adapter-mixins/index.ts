import { buildEntityBaseUrl } from './build-entity-base-url';
import { buildEntityRequest } from './build-entity-request';

export const buildEntityRestAdapterMixins = {
  buildEntityBaseUrl,
  buildEntityRequest,
};
